# Copy to .env before running:
#   cp .env.testnet.example .env && docker compose up --build -d
#
# Or pick this file directly:
#   docker compose --env-file .env.testnet.example -p module3-testnet up --build -d

# Network selector — drives the indexer CLI and the dashboard's /api/config
NETWORK=testnet

# Traefik host rule — must resolve to this VM (DNS A record)
DASHBOARD_HOST=module-3.vector.testnet.apexfusion.org

# Indexer poll interval in seconds (default 60)
INDEXER_INTERVAL=60
