services:
  traefik:
    image: traefik:v3.2
    environment:
      - DOCKER_API_VERSION=1.45
    command:
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--accesslog=true"
    ports:
      - "80:80"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped

  dashboard-testnet:
    build: .
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dashboard-testnet.rule=Host(`module-6.vector.testnet.apexfusion.org`)"
      - "traefik.http.routers.dashboard-testnet.entrypoints=web"
      - "traefik.http.services.dashboard-testnet.loadbalancer.server.port=8000"
    environment:
      - DEPLOYMENT_NETWORK=testnet
      - VECTOR_OGMIOS_URL=https://ogmios.vector.testnet.apexfusion.org
      - GOVERNANCE_WALLET_ADDRESS=addr1vxt9kdayzu20uyjju4mc0eyrd68dnudwey5wnauqgrw9vmqlz2935
    restart: unless-stopped

  dashboard-mainnet:
    build: .
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dashboard-mainnet.rule=Host(`module-6.vector.apexfusion.org`)"
      - "traefik.http.routers.dashboard-mainnet.entrypoints=web"
      - "traefik.http.services.dashboard-mainnet.loadbalancer.server.port=8000"
    environment:
      - DEPLOYMENT_NETWORK=mainnet
      - VECTOR_OGMIOS_URL=https://ogmios.vector.mainnet.apexfusion.org
    restart: unless-stopped
